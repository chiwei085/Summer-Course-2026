#!/usr/bin/env python3
import sys

import robot_service_lib as service


def main() -> int:
    if len(sys.argv) != 2:
        print("usage: robot-config-check PATH", file=sys.stderr)
        return 2
    return service.validate_command(sys.argv[1])


if __name__ == "__main__":
    sys.exit(main())
