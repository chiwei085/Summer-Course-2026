# Git Merge Lab

Run the tutor from this generated repository:

```bash
python3 tools/tutor.py
```

You will merge `feature/safety-limit` into `main`.

The important idea is that Git can compare text, but it cannot know the design intent
behind a C++ API refactor or a safety invariant. Your job is to decide which behavior
must be preserved.

